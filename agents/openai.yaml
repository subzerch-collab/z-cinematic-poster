interface:
  display_name: "Black-Gold Cinematic Poster"
  short_description: "Recompose portraits as black-gold art while preserving identity"
  default_prompt: "Use $z-cinematic-poster to recompose this portrait as a premium black-gold poster while preserving the same person."

policy:
  allow_implicit_invocation: true
