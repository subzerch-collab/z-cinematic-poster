interface:
  display_name: "Black-Gold Portrait Campaign"
  short_description: "Create polished identity-preserving black-gold portrait art"
  default_prompt: "Use $z-cinematic-poster to create polished black-gold portrait artwork with refined skin and makeup while preserving the same person, asking me to choose the aspect ratio first if needed."

policy:
  allow_implicit_invocation: true
